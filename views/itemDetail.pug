extends layout 

block content 
  h1= item.name

  if item.picture
    img(src=item.picture, alt="picture of item", height="300px" width="300px")
  p= item.description 
  p Price: #{item.price}€
  p Amount: #{item.amount}
  p Category: 
    a(href=item.category.url) #{item.category.name}

  p
    a(href=item.url+'/delete') Delete Item
  p
    a(href=item.url+'/update') Update Item